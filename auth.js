const clientReady=window.supabase&&window.KAFKA_SUPABASE_URL&&!KAFKA_SUPABASE_URL.includes("YOUR-");const client=clientReady?supabase.createClient(KAFKA_SUPABASE_URL,KAFKA_SUPABASE_ANON_KEY):null;const form=document.getElementById("loginForm"),msg=document.getElementById("loginMessage");if(client)client.auth.getSession().then(({data})=>{if(data.session)location.href="admin.html"});form?.addEventListener("submit",async e=>{e.preventDefault();msg.textContent="Signing in...";if(!client){msg.textContent="Configure Supabase first.";return}const {error}=await client.auth.signInWithPassword({email:email.value.trim(),password:password.value});if(error)msg.textContent=error.message;else location.href="admin.html";});